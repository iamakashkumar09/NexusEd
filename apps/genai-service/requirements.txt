# ─────────────────────────────────────────────────────────────────────────────
# GenAI Service — Python Dependencies
# Using >= constraints so pip resolves compatible wheels for your Python version
# ─────────────────────────────────────────────────────────────────────────────

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Configuration
pydantic-settings>=2.4.0
python-dotenv>=1.0.0

# ─── LangChain Stack ──────────────────────────────────────────────────────────
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-qdrant>=0.1.0

# LLM Providers (LangChain abstracted — swap via LLM_PROVIDER env var)
langchain-groq>=0.2.0           # Groq (default — ultra-fast inference)
langchain-google-genai>=2.0.0   # Gemini (optional)
langchain-openai>=0.2.0         # OpenAI (optional)

# ─── Embeddings (LangChain-wrapped SentenceTransformers) ─────────────────────
langchain-huggingface>=0.1.0
sentence-transformers>=3.1.0

# ─── Vector Database ──────────────────────────────────────────────────────────
qdrant-client>=1.7.0

# ─── YouTube Transcription ────────────────────────────────────────────────────
youtube-transcript-api>=0.6.2

# ─── Message Broker ───────────────────────────────────────────────────────────
aio-pika>=9.4.0                 # Async RabbitMQ client

# ─── gRPC ─────────────────────────────────────────────────────────────────────
grpcio>=1.60.0
grpcio-tools>=1.60.0            # For proto code generation

# ─── Utilities ────────────────────────────────────────────────────────────────
httpx>=0.27.0
